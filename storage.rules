rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow reading images, write requires authentication
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
